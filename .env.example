# Copy from Milestone's .env.local — same Supabase project.
# Then confirm SUPABASE_SERVICE_ROLE_KEY is set (needed to create passcode accounts).
# BLOB_READ_WRITE_TOKEN is the same Vercel Blob token as Milestone (profile photos).

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
BLOB_READ_WRITE_TOKEN=
